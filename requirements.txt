# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
aiofiles>=23.0.0

# Validação e configuração
pydantic>=2.6.0
pydantic-settings>=2.2.0
email-validator>=2.1.0
python-dotenv>=1.0.0

# Segurança
bcrypt>=4.0.0,<4.2.0
python-jose[cryptography]>=3.3.0

# Banco de dados — psycopg v3 cobre sync (Alembic) e async (runtime)
SQLAlchemy>=2.0.0
psycopg[binary]>=3.1.0
greenlet>=3.0.0

# Migrations
alembic>=1.13.0

# HTTP client (gateway proxy)
httpx>=0.27.0
